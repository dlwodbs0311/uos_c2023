#include <stdio.h>
#include "header1.h"

int main(void) {

	lcm(3, 4); //lcm함수에 (3,4) 대입한 값 출력

	gcd(12, 15); //gcd함수에 (12,15) 대입한 값 출력

	prime_number(7); //prime_number함수에 (7) 대입한 값 출력

	lcmm(); //lcm함수 실행

	gcdd(); //gcdd함수 실행

	prime_numberr(); // prime_number함수 실행
}